# Supabase → Project Settings → API
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_SERVICE_KEY=tu-service-role-key   # NUNCA la anon key, y NUNCA en el frontend

# Resend → dashboard.resend.com → API Keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
EMAIL_REMITENTE=SentimentBrand <onboarding@resend.dev>

# Genera uno con: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=cambia-este-valor-por-uno-generado-aleatoriamente

FRONTEND_ORIGIN=http://localhost:5173
